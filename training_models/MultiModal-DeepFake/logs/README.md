# HAMMER Logs

Runtime logs are written here by `run_hammer_small_train_eval.sh`.

Large or actively changing `*.log` files are intentionally ignored by git. When an experiment finishes, copy key metrics into `docs/` or commit a small summary file instead of committing the full live log.
